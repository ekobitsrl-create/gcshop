"use client";
import { useEffect, useState } from "react";
export function PayPalComplete({token,orderNumber}:{token:string;orderNumber:string}){const [message,setMessage]=useState("Conferma del pagamento in corso…");useEffect(()=>{void (async()=>{const response=await fetch("/api/payments/paypal/capture",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token,orderNumber})});const payload=await response.json();if(response.ok)window.location.href=payload.redirectUrl;else setMessage(payload.error??"Pagamento non confermato.");})();},[token,orderNumber]);return <p>{message}</p>}
